<!-- Must position footer at the bottom of the body!-->
<!-- TODO: link to the correct paths-->
<div class="footer-wrapper">
    <!-- Uncomment these conditionals and correct the paths -->
    <% user_page = current_page?(my_dashboard_path) ? "active" : "" %>
    <% groups_page = current_page?(groups_path) ? "active" : "" %>
    <% challenges_page = current_page?(challenges_path) ? "active" : "" %>
    <%= link_to icon("far", "user"), my_dashboard_path, class: "footer-link #{user_page}" %>
    <%= link_to icon("fas", "users"), groups_path, class: "footer-link #{groups_page}" %>
    <%= link_to icon("far", "list-alt"), challenges_path, class: "footer-link #{challenges_page}" %>
</div>
